.container
   h2= '模块状态'
   .lead= bundles['status']
   include update_form.html
   table.table.table-striped.table-bordered.table-condensed
     thead
       tr
         -for(key in bundles['data'][0])
          if(key != 'stateRaw' && key != 'fragment' && key != 'symbolicName' && key != 'category')
             th= key
     tbody
      -for (id in bundles['data'])
       tr
         -for(key in bundles['data'][id])
            if(key != 'stateRaw' && key != 'fragment' && key != 'symbolicName' && key != 'category')
              td= bundles['data'][id][key]
         td
           .dropdown
             a.dropdown-toggle(href='#', data-toggle='dropdown')
               |  操作
               b.caret
             ul.dropdown-menu
               li.nav-header 状态
               li.divider
               li
                 a(href='/console/start?id=' + bundles['data'][id]['id']) 开启
               li
                 a(href='/console/stop?id=' + bundles['data'][id]['id']) 关闭